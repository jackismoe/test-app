import React from 'react';
import ReactDOM from 'react-dom';
import StateComponent from './StateREADME';

ReactDOM.render(
  <React.StrictMode>
    <StateComponent />
  </React.StrictMode>,
  document.getElementById('root')
);